# Application environment (development, production)
NODE_ENV=development

# Port number for the application server
# Default: 3000
PORT=3000

# MySQL database connection URL
# Format: mysql://username:password@host:port/database_name
# Example: mysql://root:password@localhost:3306/ebraz_finance
DATABASE_URL=mysql://username:password@localhost:3306/ebraz_finance

# Redis URL for caching and job queues
# Format: redis://hostname:port
# Example: redis://localhost:6379
REDIS_URL=redis://hostname:port

# Redis Time-to-live time for caching in milliseconds
# Format: 1800000 (30 mins)
REDIS_CACHE_TTL=1800000

# Secret key for JWT token signing and verification
# Generate a strong, random string (at least 32 characters)
# Example: your-super-secret-jwt-key-here-make-it-very-long-and-random
JWT_SECRET=your-super-secret-jwt-key-here-make-it-very-long-and-random

# JWT token expiration time
# Format: number + unit (s=seconds, m=minutes, h=hours, d=days)
# Examples: 1h, 24h, 7d, 3600s
# Default: 10m
JWT_EXPIRES_IN=10m

# OpenAI API key for AI-powered financial insights
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key-here

# OpenAI model to use for generating insights
# Recommended: gpt-5, gpt-4o-mini
# Default: gpt-4o-mini
OPENAI_MODEL=gpt-4o-mini

# Note: Exchange rates are fetched from Bitpin API (no API key required)
# In times of high usage rate, fetch API keys from BitPin
# Implementation for that is required in the code...
# The service automatically fetches USD/IRT exchange rates from:
# https://api.bitpin.org/api/v1/mkt/tickers/
